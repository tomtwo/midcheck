{
  "name": "midcheck",
  "version": "0.1.0",
  "description": "check a website for a keyword to show up, then notify owner with SMS",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node app.js"
  },
  "author": "tomtwo",
  "repository": "tomtwo/midcheck",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^1.1.0",
    "request": "^2.50.0",
    "twilio": "^1.10.0"
  },
  "config": {
    "checkIntervalSeconds": 30
  }
}
